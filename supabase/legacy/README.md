# Legacy SQL (reference only)

These files were applied manually via the Supabase SQL Editor before the CLI migration workflow.

**Do not run them on production again** if `supabase db push` has already applied the equivalent migrations under `../migrations/`.

Use this folder only to diff or recover historical DDL. All new schema changes belong in `supabase/migrations/`.
