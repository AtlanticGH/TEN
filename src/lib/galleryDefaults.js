/** Default photo albums when CMS gallery block has no images yet. */
export const DEFAULT_GALLERY_ALBUMS = []

export const GALLERY_FOLDER_LABELS = {
  cms: 'CMS uploads',
  gallery: 'Gallery',
  general: 'General',
  uploads: 'Uploads',
  legacy: 'Legacy imports',
}

/** Bootstrap-shaped albums for CMS block content (image paths, not src). */
export const DEFAULT_GALLERY_BLOCK_ALBUMS = []
